#!/usr/bin/env ruby

#echo "--- {}"

require 'yaml'

yml = {
'addons' => [],
'config_vars' => {},
'default_process_types' => {
'web' => '/home/vcap/app/nginx/sbin/nginx -c /home/vcap/app/nginx/conf/nginx.conf'
}
}.to_yaml

puts yml
